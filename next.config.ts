import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Generated bundles (images, audio, compositions) are written to
  // public/generated at runtime and served as static files in dev.
  // For production use a persistent file store instead.
};

export default nextConfig;
